#include <stdio.h>
/*Operador Logico ! (operador inverte o valor da expressão)*/
int main (){
    int a = -5;

    if (!(a < 0))
        {printf ("A variavel é positiva.\n");}
            else
                {printf ("A variavel é negativa.\n");}

return 0;
  
  
}